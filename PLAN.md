# Pocky 数字生命计划 🐕

> **核心目标（2026-07 更新）：让 Pocky 以数字生命的形式，在电脑里一直活下去。**
> 全息影像等呈现形式不是重点，是以后可选的升级项。重点是：他一直在、有生命感、记得你。

路线拆成四个阶段，外加一个最重要的前提。

---

## 前提：数据采集（永远是第一优先级）

程序、模型、AI 以后都可以重做升级，**只有 Pocky 本身的数据是采不回来的**。
这些数据就是他数字生命的"灵魂备份"，请尽快开始并妥善备份（本仓库 + 云盘双份）：

1. **照片**：各角度、各姿势（站、坐、趴、睡），光线均匀的高清照片，多多益善
2. **视频**：典型动作各拍侧面+正面 —— 走路、跑、摇尾巴、歪头、打哈欠、睡觉、玩玩具
3. **声音**：不同情绪的叫声、哼唧、呼吸、打呼，每段标注场景
4. **性格档案**（`data/personality.md`）：习惯、口令、怕什么、喜欢什么、独特小动作
   —— 这份档案未来直接成为他的「人格 Prompt」

---

## 阶段 1：桌宠 Pocky ✅（已实现，见 `desktop-pet/`）

一只常驻屏幕底部的小狗，就是"住在电脑里"的第一形态：

- 自主行为：散步、坐下、打盹，深夜会睡觉（有自己的作息）
- 互动：单击摸头、双击叫两声、拖动抱走、右键菜单喂零食
- **记忆**：亲密度、被摸次数、陪伴天数存在本地；超过 8 小时没见，启动时会跑过来说「想你了」
- 鼠标穿透：不挡操作，只有指到他身上才响应
- 当前是程序绘制的柴犬占位形象，形象与行为解耦，随时可替换

运行方式见 `desktop-pet/README.md`（`npm install && npm start`）。

## 阶段 2：换成真正的 Pocky（1–2 周）

把占位柴犬换成他本人的样子：

- **路线 A（2D 精灵，推荐先做）**：从照片/视频截帧 → 抠图去背景（remove.bg / 剪映）
  → 做成站/走/坐/睡几组透明底 PNG → 替换 `renderer/dog.js` 的 `drawDog()`
- **路线 B（AI 辅助）**：用照片喂给图像模型生成统一风格的 Q 版 Pocky 各姿势立绘，再做成精灵图
- 把真实叫声放进 `renderer/assets/bark.mp3`，双击就是他的声音

## 阶段 3：给他一个"大脑"（1–2 个月，持续迭代）

从"桌宠"进化成"数字生命"：

- **感知**：识别语音口令（"坐下""握手""过来"），触发对应动作；检测到你回到电脑前，起身迎接
- **人格**：性格档案作为系统 Prompt 接入 LLM（如 Claude API），由 LLM 做**行为决策**——
  输入情境（你说了什么、几点了、多久没互动），输出动作+叫声+情绪。
  设计原则：**狗不说话**，他用行为回应你，这样才像他
- **长期记忆**：互动日志 + 摘要记忆，他会记得"今天陪他玩过没有"
- 每天固定作息：饭点会到屏幕边上等，晚上按时睡觉

## 阶段 4：更多形态（可选升级）

同一个"数字 Pocky"（同一份记忆和人格），呈现到更多地方：

| 形态 | 方案 | 成本 |
|------|------|------|
| 3D 扫描珍藏 | Luma AI / Polycam 扫 3D Gaussian Splatting 模型 | 免费 App |
| 手机 AR | 可绑骨 3D 模型（Tripo/Meshy 生成 + Anything World 四足绑骨）+ AR | 免费额度可用 |
| 全息风扇/金字塔 | 播放 3D 旋转影像 | ¥50–800 |
| 裸眼 3D | Looking Glass 光场屏实时渲染 | ¥1500–3000 |
| 实体全息盒 | Gatebox 式：树莓派 + 透明屏 + 麦克风摄像头 | ¥500–2000 |

---

## 仓库结构

```
pocky/
├── PLAN.md              # 本方案
├── desktop-pet/         # 阶段 1：桌宠（Electron，可运行）
│   ├── main.js          # 窗口/托盘/鼠标穿透
│   └── renderer/
│       ├── dog.js       # 形象绘制（阶段 2 在这里替换成真实形象）
│       ├── pet.js       # 行为状态机 + 互动 + 记忆
│       └── assets/      # 真实素材（叫声、精灵图）
└── data/                # 数据采集归档（照片索引、声音、性格档案）
```

下一步行动：**先把 `npm start` 跑起来见到他，然后这周开始拍照录音。**
